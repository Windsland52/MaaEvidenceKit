[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "maa-diagnostic-expert"
version = "0.1.0"
description = "Python-first diagnostic agent for Maa projects"
readme = "README.md"
requires-python = ">=3.13,<3.14"
license = "MIT"
authors = [{ name = "Windsland52" }]
dependencies = [
  "langchain>=1,<2",
  "langchain-core>=1,<2",
  "langgraph>=1,<2",
  "pydantic>=2.12,<3",
]

[project.optional-dependencies]
openai = ["langchain-openai>=1,<2"]
anthropic = ["langchain-anthropic>=1,<2"]
bedrock = ["langchain-aws>=1,<2"]
deepseek = ["langchain-deepseek>=1,<2"]
google = ["langchain-google-genai>=4,<5"]
groq = ["langchain-groq>=1,<2"]
mistral = ["langchain-mistralai>=1,<2"]
ollama = ["langchain-ollama>=1,<2"]
xai = ["langchain-xai>=1,<2"]
models = [
  "langchain-anthropic>=1,<2",
  "langchain-aws>=1,<2",
  "langchain-deepseek>=1,<2",
  "langchain-google-genai>=4,<5",
  "langchain-groq>=1,<2",
  "langchain-mistralai>=1,<2",
  "langchain-ollama>=1,<2",
  "langchain-openai>=1,<2",
  "langchain-xai>=1,<2",
]

[project.scripts]
maa-diagnostic-expert = "maa_diagnostic_expert.cli:main"
maa-generate-contracts = "maa_diagnostic_expert.contracts:main"

[dependency-groups]
dev = [
  "pyright>=1.1.400",
  "pytest>=8,<10",
  "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/maa_diagnostic_expert"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
line-ending = "lf"

[tool.pyright]
include = ["src", "tests"]
extraPaths = ["src"]
pythonVersion = "3.13"
typeCheckingMode = "strict"
